name: Build ARMSX2 NEXT 128MB Test APK

on:
  push:
    branches:
      - build/armsx2-128mb-apk
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - run: echo workflow-ok
